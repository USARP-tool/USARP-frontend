import { PageNumbers } from "./PageNumbers";
import { PaginationRoot } from "./Pagination";
import { PerPageSelector } from "./PerPageSelector";

export const Pagination = {
  Root: PaginationRoot,
  Selector: PerPageSelector,
  Numbers: PageNumbers,
};
