import { CheckboxGroupCaption } from "./CheckboxGroupCaption";
import { CheckboxGroupRoot } from "./CheckboxGroupRoot";

/**
 *
 * @returns {Object} Retorna um objeto com os subcomponentes:
 * - Root: Container principal para um grupo de Checkbox
 * - Caption: Legenda pequena que acompanha o grupo de checkbox
 */
export const CheckboxGroup = {
  Root: CheckboxGroupRoot,
  Caption: CheckboxGroupCaption,
};
